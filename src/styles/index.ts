import {StyleSheet} from 'react-native';

const stylesheet = StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: -50,
  },
  modalContainer: {
    flex: 1,
    paddingBottom: 0,
    paddingTop: -50,
  },
});

export default stylesheet;
